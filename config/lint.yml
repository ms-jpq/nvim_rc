---
- bin: lint-py
  type: fs
  args:
    - "%"
  filetypes:
    - python
  install:
    pip:
      - mypy
      - pylint

- bin: shellcheck
  type: fs
  args:
    - --
    - "%"
  filetypes:
    - sh
    - zsh

- bin: markdownlint
  type: stream
  args:
    - --stdin
  filetypes:
    - markdown
  install:
    npm:
      - markdownlint-cli

- bin: write-good
  type: stream
  args:
    - "%"
  filetypes:
    - markdown
  install:
    npm:
      - write-good
